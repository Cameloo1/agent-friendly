{
  "name": "is-it-agent-ready-release",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-harness release package for the unofficial Is It Agent Ready skill.",
  "type": "module",
  "engines": {
    "node": ">=22 <27"
  },
  "scripts": {
    "test": "node --test",
    "validate": "node ./scripts/validate-package.mjs",
    "smoke:offline": "node ./scripts/smoke-test.mjs",
    "test:live": "node ./scripts/live-fixture.mjs",
    "check": "npm run validate && npm test && npm run smoke:offline"
  },
  "license": "MIT"
}
